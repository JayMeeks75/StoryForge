# Dataset Folders

Add real dataset files to these folders:

- `english-corpora`
- `storywars`
- `common-corpora`
- `tiny-stories`
- `deepmind`

The server reads `.txt`, `.md`, `.jsonl`, `.json`, and `.csv` files from those folders and uses short local snippets as grounding context for story generation.
